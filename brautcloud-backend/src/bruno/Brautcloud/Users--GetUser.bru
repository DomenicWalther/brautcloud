meta {
  name: Users::GetUser
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/user
  body: none
  auth: bearer
}

auth:bearer {
  token: {{$processEnv BRAUTCLOUD_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
